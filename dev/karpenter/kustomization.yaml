helmCharts:
  - repo: oci://public.ecr.aws/karpenter
    name: karpenter
    version: "1.7.0"
    includeCRDs: true
    namespace: karpenter
    releaseName: karpenter
    valuesFile: values.yaml

resources:
  - default.yaml
